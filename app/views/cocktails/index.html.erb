<h1>Cocktails</h1>
<span><%= link_to 'Add a cocktail', new_cocktail_path %></span>

<ul>
  <% @cocktails.each do |cocktail| %>
    <li>
      <%= link_to cocktail_path(cocktail.id) do %>
        <%= cocktail.name %>
      <% end %>
     </li>
  <% end %>
</ul>
